<!DOCTYPE html>
<html>
  <head>
    <title>ProductStore</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag 'application', media: 'all' %>

  </head>
  <body>
    <% flash.each do |name, msg| %>
      <% if msg.is_a?(String) %>
        <div class="alert text-center alert-<%= name == 'notice' || name == 'success' ? "success" : "danger" %> alert-dismissible fade show" role="alert">
          <%= msg %>
        </div>
      <% end %>
    <% end %>
    <%= yield %>
    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
  </body>
</html>